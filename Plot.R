#create stepped bar charts without spaces(like Histogram)

barplot(y,space=0,ylim=c(0,0.7),names.arg=c(0,1,2,3,4), main=c("Probability Density"),xlab=c("Buckets"),y=c("Probability"),font.lab=2,col="dodgerblue" )
